/* import Swiper from "swiper";

setTimeout(() => {
    let next_btns = document.getElementsByClassName('next');
    for (let i = 0; i < next_btns.length; i++) {
        next_btns.item(i).addEventListener('click', () => {
            Swiper.navigation.nextEl();
        })
    }
}, 1) */